import Routes from "./routes";

export default function App(props) {
  return <Routes />;
}

// redux 管理状态
// react-redux 传递状态，redux与react的绑定库
// redux-thunk\redux-saga 中间件 处理异步
// react-router 处理路由

// setTimeout(() => {
//   // dispatch()
// }, 10000);
